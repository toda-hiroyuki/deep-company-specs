# Deep Experience 事業者向け管理画面 仕様書

**作成日**: 2026-05-18  
**更新日**: 2026-05-19  
**対象システム**: Deep Experience 事業者向け管理画面  
**リポジトリ**: deep-company-specs  
**参照ソースコード**: `source-code/deep-experience`

---

## 目次

1. [概要](#1-概要)
2. [システム構成](#2-システム構成)
3. [機能一覧](#3-機能一覧)
4. [詳細仕様](#4-詳細仕様)
   - 4.1 [ログイン・認証](#41-ログイン・認証)
   - 4.2 [共通仕様（ログイン後）](#42-共通仕様ログイン後)
   - 4.3 [ダッシュボード](#43-ダッシュボード)
   - 4.4 [施設情報管理](#44-施設情報管理)
   - 4.5 [アクティビティ管理](#45-アクティビティ管理)
   - 4.6 [スケジュール管理](#46-スケジュール管理)
   - 4.7 [予約管理](#47-予約管理)
   - 4.8 [支払い確認](#48-支払い確認)
   - 4.9 [通知設定](#49-通知設定)
   - 4.10 [アカウント設定](#410-アカウント設定)
5. [非機能要件](#5-非機能要件)
6. [セキュリティ要件](#6-セキュリティ要件)
7. [データモデル（追加・変更）](#7-データモデル追加・変更)
8. [画面一覧・遷移](#8-画面一覧・遷移)

---

## 1. 概要

### 1.1 システムの目的

本システムは、旅行予約サイト **Deep Experience** にガイドツアー用アクティビティを提供する施設（以下「事業者」）が、自社アクティビティの管理・予約確認・収益確認を行うための管理画面である。

### 1.2 利用者

| 利用者 | 説明 |
|---|---|
| **事業者ユーザー** | アクティビティを提供する施設の担当者。本仕様書の主対象。 |
| **Deep Experience 運営者** | 事業者アカウント発行・アクティビティ公開承認・言語マスター管理を行う。既存の Admin 管理画面で対応。 |

### 1.3 前提・制約

- 本管理画面は既存の Deep Experience バックエンド（Next.js 15 / Prisma / PostgreSQL）に機能追加する形で実装する
- 事業者アカウントは Deep Experience 運営者が既存の Admin 管理画面から発行する
- アクティビティの公開・非公開は Deep Experience 運営者が承認・操作する（事業者は登録・編集のみ）
- 多言語対応の「対応言語マスター」は Deep Experience 運営者が管理する

---

## 2. システム構成

### 2.1 技術スタック（既存踏襲）

| 区分 | 技術 |
|---|---|
| フレームワーク | Next.js 15（App Router）|
| データベース | PostgreSQL（Supabase）/ SQLite（開発環境）|
| ORM | Prisma 6 |
| 認証 | JWT（jsonwebtoken）|
| 画像処理 | sharp（WebP変換・サムネイル生成）|
| ファイルストレージ | Supabase Storage（本番）/ ローカルファイルシステム（開発）|
| i18n | i18next / react-i18next |
| UI | Tailwind CSS / Lucide React |
| メール送信 | SendGrid（トランザクションメール）※将来的な通知チャンネル追加に備え抽象化して実装 |

### 2.2 新設ルート構成

```
/apps/backend/src/app/
├── operator/                    # 事業者向け管理画面（新設）
│   ├── login/page.tsx
│   ├── dashboard/page.tsx
│   ├── facility/page.tsx        # 施設情報
│   ├── activities/
│   │   ├── page.tsx             # アクティビティ一覧
│   │   ├── new/page.tsx
│   │   └── [activityId]/page.tsx
│   ├── schedules/page.tsx       # スケジュール管理
│   ├── bookings/
│   │   ├── page.tsx             # 予約一覧
│   │   └── calendar/page.tsx   # 予約カレンダー
│   ├── payments/page.tsx        # 支払い確認
│   └── settings/page.tsx       # アカウント設定・通知設定

/apps/backend/src/app/api/v1/operator/   # 事業者向けAPI（新設）
├── auth/
│   ├── login/route.ts
│   └── password-reset/route.ts
├── facility/route.ts
├── activities/
│   ├── route.ts
│   └── [activityId]/
│       ├── route.ts
│       ├── schedules/route.ts
│       ├── capacity-rules/route.ts
│       ├── pricing/route.ts
│       └── media/route.ts
├── bookings/
│   ├── route.ts
│   └── [bookingId]/
│       ├── route.ts
│       └── status/route.ts
├── payments/route.ts
└── notifications/route.ts
```

---

## 3. 機能一覧

| # | 機能 | 区分 | 説明 |
|---|---|---|---|
| 1 | ログイン・パスワードリセット | **必須** | メールアドレス+パスワード認証 |
| 2 | ログアウト | **必須** | セッション終了 |
| 3 | ダッシュボード | **必須** | 今月の予約数・売上サマリー表示 |
| 4 | 施設情報管理 | **必須** | 自施設の基本情報（住所・連絡先等）編集 |
| 5 | アクティビティ登録・編集 | **必須** | 多言語対応、画像アップロード |
| 6 | 料金設定 | **必須** | 参加者種別ごとの1人あたり利用料金設定 |
| 7 | スケジュール管理 | **必須** | 開催日・時間・定員登録（曜日単位指定可）|
| 8 | 休業日・臨時クローズ管理 | **必須** | 特定日のスケジュール例外設定 |
| 9 | 予約カレンダー | **必須** | 月次カレンダー形式で予約確認 |
| 10 | 予約一覧・検索 | **必須** | 日付・受注番号・予約者情報で検索 |
| 11 | 予約CSV エクスポート | **必須** | 検索結果を CSV でダウンロード |
| 12 | 予約承認・拒否 | **必須** | 入ってきた予約のOK/NG操作 |
| 13 | 予約通知 | **必須** | 新規予約時の通知（メール＋画面内）|
| 14 | 支払い確認 | **必須** | 月次の確定済み支払い確認（事業者毎の支払いサイクル設定）|
| 15 | 通知設定 | **必須** | 通知方法・タイミングのカスタマイズ |
| 16 | アカウント設定 | **必須** | パスワード変更・メールアドレス変更 |

---

## 4. 詳細仕様

### 4.1 ログイン・認証

#### 4.1.1 ログイン画面

**画面URL**: `/operator/login`

**入力項目**:

| 項目 | 種別 | バリデーション |
|---|---|---|
| メールアドレス | text（email） | 必須・メール形式 |
| パスワード | password | 必須・8文字以上 |

**ボタン**:
- ログインボタン
- 「パスワードをお忘れの方はこちら」リンク

**認証フロー**:
1. 入力値を `POST /api/v1/operator/auth/login` へ送信
2. サーバー側でメールアドレス・パスワード照合（bcrypt）
3. 成功時: JWT（有効期限24時間）を発行し、`httpOnly` Cookie にセット
4. ログイン後: ダッシュボード画面（`/operator/dashboard`）へリダイレクト
5. 失敗時: 「メールアドレスまたはパスワードが正しくありません」を表示（3回連続失敗で5分間アカウントロック）

**APIエンドポイント**: `POST /api/v1/operator/auth/login`
```json
Request:  { "email": "string", "password": "string" }
Response: { "token": "string", "operator": { "id": "string", "name": "string", "email": "string" } }
```

#### 4.1.2 パスワードリセット

**画面URL**: `/operator/login/reset-password`

**フロー**:

```
[STEP 1] メールアドレス入力
  → POST /api/v1/operator/auth/password-reset/request
  → リセット用トークン（有効期限1時間）をDBに保存
  → 登録メールアドレスへパスワードリセットメール送信
  → 「メールをご確認ください」メッセージ表示

[STEP 2] メール内リンクからアクセス
  → /operator/login/reset-password?token=xxx
  → GET /api/v1/operator/auth/password-reset/verify?token=xxx でトークン有効性確認

[STEP 3] 新パスワード入力
  → 新パスワード（8文字以上）・確認用パスワード入力
  → POST /api/v1/operator/auth/password-reset/complete
  → 成功時: ログイン画面へリダイレクト
```

**注意事項**:
- 未登録のメールアドレスを入力した場合も、セキュリティのため「メールをご確認ください」を表示（ユーザー存在の有無を漏洩しない）
- トークン使用後は即座に無効化

#### 4.1.3 アカウント発行

- 事業者アカウントの新規発行は **Deep Experience 運営者の Admin 管理画面** から行う
- 発行時に事業者メールアドレスへ「初回パスワード設定メール」を送信
- 運営者側では事業者アカウントに対して管理する施設（Spot）を紐付ける

---

### 4.2 共通仕様（ログイン後）

#### 4.2.1 データアクセス制御

- 事業者ユーザーは **自社に紐付くデータのみ**参照・編集可能
- 他事業者のアクティビティ・予約・支払い情報は閲覧不可
- 全APIエンドポイントでJWT検証を行い、事業者IDによるスコープ制限を適用

#### 4.2.2 ナビゲーション

**サイドバーメニュー**:
1. ダッシュボード
2. 施設情報
3. アクティビティ管理
4. スケジュール管理
5. 予約管理（サブメニュー: カレンダー / 一覧）
6. 支払い確認
7. 設定（サブメニュー: アカウント / 通知）
8. ログアウト

**ヘッダー**:
- 事業者名表示
- 未読通知件数バッジ（ベルアイコン）
- 現在ログイン中のユーザー名

#### 4.2.3 ログアウト

- サイドバー「ログアウト」クリック → 確認ダイアログ表示
- 確認後: `POST /api/v1/operator/auth/logout` でサーバー側セッション無効化 → ログイン画面へリダイレクト
- JWTはCookieから削除

---

### 4.3 ダッシュボード

**画面URL**: `/operator/dashboard`

**表示内容**:

| 項目 | 説明 |
|---|---|
| 今月の予約数 | 当月の確定予約件数（前月比） |
| 今月の確定売上 | 当月の確定済み売上合計（前月比） |
| 承認待ち予約 | PENDING 状態の予約件数（即アクションリンク付き）|
| 直近7日の予約推移 | 折れ線グラフ（日別予約件数）|
| 直近の予約 | 最新5件の予約情報（日時・アクティビティ名・人数・ステータス）|
| 今日のスケジュール | 本日開催のアクティビティと予約状況 |

---

### 4.4 施設情報管理

**画面URL**: `/operator/facility`

**編集可能項目**:

| 項目 | 種別 | 説明 |
|---|---|---|
| 施設名 | テキスト（多言語） | 言語マスターに応じた言語数分入力欄を表示 |
| 所在地 | テキスト＋地図 | 住所入力でジオコーディング、地図ピン確認 |
| 電話番号 | テキスト | |
| 営業時間 | テキスト | |
| 施設画像 | 画像アップロード | jpg/png/webp → WebP変換（後述）|
| 施設紹介文 | テキストエリア（多言語） | |

**注意**:
- 施設の位置情報（緯度・経度）はアクティビティ検索の地図表示に使用されるため正確に設定する必要がある
- 施設名・紹介文は多言語入力対応（言語マスターで有効化された言語のみ表示）

---

### 4.5 アクティビティ管理

**画面URL**: `/operator/activities`

#### 4.5.1 アクティビティ一覧

**表示項目**:
- アクティビティ名（ログインユーザーのロケール優先）
- ステータス（下書き / 審査中 / 公開中 / 非公開）
- 代表画像サムネイル
- 基本料金
- 作成日時 / 更新日時

**操作**:
- 新規作成ボタン
- 行クリックで詳細・編集画面へ遷移
- ステータスフィルター

**ステータス定義**:

| ステータス | 説明 |
|---|---|
| 下書き | 事業者が作成・編集中。非公開。 |
| 審査中 | 事業者が運営者へ公開申請済み。編集不可。 |
| 公開中 | 運営者が承認・公開済み。ゲスト向けサイトに表示。 |
| 非公開 | 運営者が非公開にした状態。 |

#### 4.5.2 アクティビティ登録・編集画面

**画面URL**: `/operator/activities/new`、`/operator/activities/[activityId]`

**タブ構成**:
1. **基本情報**
2. **料金設定**
3. **画像**
4. **スケジュール設定**（4.6 を参照）

---

##### タブ1: 基本情報

**多言語テキスト入力**:

言語マスターで有効化された言語ごとに以下の入力欄を表示。

| 項目 | 種別 | バリデーション |
|---|---|---|
| アクティビティ名 | テキスト（多言語） | 各言語で必須 |
| 概要 | テキストエリア（多言語） | |
| 詳細説明 | リッチテキスト or テキストエリア（多言語） | |
| 含まれるもの | テキストエリア（多言語） | 含まれるサービスを記述 |
| 含まれないもの | テキストエリア（多言語） | |
| 持ち物・注意事項 | テキストエリア（多言語） | |
| キャンセルポリシー | テキストエリア（多言語） | |

**その他基本情報**:

| 項目 | 種別 | バリデーション |
|---|---|---|
| アクティビティ種別 | セレクト（グループ / プライベート）| 必須 |
| 所要時間（分） | 数値 | 必須・正の整数 |
| 集合場所 | テキスト（多言語） | |
| 最小催行人数 | 数値 | 必須・1以上 |
| 最大催行人数 | 数値 | 必須・最小催行人数以上 |

**データ保存仕様**:
- 多言語テキストは既存の `Tour` モデルの多言語フィールド（例: `nameJa` / `nameEn`）に準拠
- 対応言語数が増減した場合、言語マスターに連動してUIの入力欄を動的生成する

---

##### タブ2: 料金設定

**参加者種別ごとの料金設定**（`PricingCategory` / `Rate` / `RatePrice` モデル参照）:

- 料金プラン（例: 標準料金）を作成し、参加者種別（大人・子ども・幼児など）ごとに料金を設定
- 参加者種別は Deep Experience 運営者が管理するマスターから選択
- 1アクティビティに複数の料金プランを設定可能（例: 繁忙期料金、早割料金）

| 項目 | 種別 | 説明 |
|---|---|---|
| 料金プラン名 | テキスト | 例: 標準料金 |
| 適用期間 | 日付範囲 | 未設定の場合は常時適用 |
| 種別別料金 | 数値（円） | 参加者種別ごとに入力 |
| 通貨 | セレクト | デフォルト: 日本円（JPY）|

**注意**:
- 料金変更後も **Deep Experience 運営者による公開承認が必要**
- 公開前の料金変更は下書き保存のみ可能

---

##### タブ3: 画像

**アップロード仕様**:

| 項目 | 仕様 |
|---|---|
| 許容フォーマット | jpg / jpeg / png / webp |
| 最大ファイルサイズ | 10MB/枚 |
| 最大枚数 | 20枚 |
| WebP変換 | jpg・png でアップロードされた場合、`sharp` ライブラリで WebP に自動変換してから保存 |
| サムネイル生成 | アップロード時に自動生成（既存 `thumbnail.ts` 利用）|

**UI**:
- ドラッグ&ドロップ対応のアップロードエリア
- プレビュー表示（サムネイル一覧）
- 代表画像（メイン画像）の設定（並び替え対応）
- 画像削除ボタン

**実装参照**:
- 既存の `POST /api/v1/admin/media/upload` エンドポイントおよび `storage/factory.ts` を事業者向けに拡張

---

### 4.6 スケジュール管理

**画面URL**: `/operator/schedules`（アクティビティ詳細のタブ4からも遷移）

#### 4.6.1 スケジュール（定期ルール）設定

開催日・時間・定員を登録する。手間を最小化するため、繰り返しルール（`CapacityRule`）を中心に設計する。

**ルール種別**:

| 種別 | 説明 | 想定ユースケース |
|---|---|---|
| **曜日繰り返し（WEEKLY）** | 毎週特定曜日・時間に開催 | 例: 毎週月・水・金 10:00 |
| **年間繰り返し（YEARLY）** | 毎年同じ月日に開催 | 例: 毎年8月15日 |
| **期間指定（RANGE）** | 特定期間中の毎日または曜日指定 | 例: 7月1日〜8月31日の毎日 |
| **単発（SINGLE）** | 特定日時のみ開催 | 例: 2026年6月10日のみ |

**WEEKLY ルール入力項目**:

| 項目 | 種別 | 説明 |
|---|---|---|
| 対象アクティビティ | セレクト | |
| 開催曜日 | チェックボックス複数選択 | 月〜日 |
| 開始時刻 | 時刻（HH:MM） | |
| 終了時刻 or 所要時間 | 時刻 or 分数 | どちらかを指定 |
| 最小催行人数 | 数値 | |
| 最大定員 | 数値 | |
| 適用開始日 | 日付 | |
| 適用終了日 | 日付（任意） | 未設定で無期限 |
| 予約締切 | 開催X時間前 | 例: 24時間前 |
| 1日最大出発回数 | 数値（任意） | |

**一括スケジュール生成**:
- ルール保存後、指定期間内のスケジュール一覧をプレビュー表示し確認後に確定する
- 既存スケジュールと重複する場合は警告表示

#### 4.6.2 休業日・臨時クローズ（CloseOut）

特定日のスケジュールを例外的に非公開にする機能。

**入力項目**:

| 項目 | 種別 |
|---|---|
| 対象アクティビティ | セレクト（全アクティビティ or 個別選択）|
| クローズ日 | 日付（単日 or 期間指定）|
| クローズ時間帯 | 全日 or 時間帯指定 |
| 理由メモ | テキスト（内部管理用）|

---

### 4.7 予約管理

#### 4.7.1 予約カレンダー

**画面URL**: `/operator/bookings/calendar`

**表示仕様**:

| 項目 | 内容 |
|---|---|
| 表示形式 | 月次カレンダー（週次切替可能）|
| 日付セル表示 | その日の予約件数・合計人数 |
| 予約イベント | クリックで予約詳細ポップアップ |
| 色分け | PENDING（黄）/ CONFIRMED（緑）/ CANCELLED（灰）/ IN_PROGRESS（青）|

**操作**:
- 前月・次月ナビゲーション
- 「今日」ボタン
- アクティビティ別フィルター
- 日付クリック → その日の予約一覧へ絞り込み

#### 4.7.2 予約一覧

**画面URL**: `/operator/bookings`

**表示項目**:

| 列 | 説明 |
|---|---|
| 受注番号 | |
| 予約日時 | 予約が入った日時 |
| アクティビティ名 | |
| 開催日時 | |
| 予約者名 | |
| 人数 | 参加者種別と人数内訳 |
| 合計金額 | |
| ステータス | PENDING / CONFIRMED / CANCELLED / IN_PROGRESS / COMPLETED |
| アクション | 詳細ボタン / 承認・拒否ボタン |

**検索・フィルター**:

| 検索項目 | 種別 |
|---|---|
| 日付範囲（開催日 or 予約日）| 日付レンジピッカー |
| 受注番号 | テキスト |
| 予約者名 | テキスト |
| 予約者メールアドレス | テキスト |
| アクティビティ | セレクト |
| ステータス | チェックボックス複数選択 |

**ページネーション**: 20件/ページ（設定可能）

**CSV エクスポート**: 検索結果（絞り込み後の全件）を CSV でダウンロード可能。出力項目: 受注番号・予約日時・開催日時・アクティビティ名・予約者名・メールアドレス・参加人数・合計金額・ステータス

#### 4.7.3 予約詳細

**表示項目**:

| セクション | 内容 |
|---|---|
| 予約基本情報 | 受注番号・予約日時・ステータス |
| 予約者情報 | 氏名・メールアドレス・電話番号（あれば）|
| 参加者情報 | 参加者種別・人数・氏名（入力あれば）|
| アクティビティ情報 | アクティビティ名・開催日時・集合場所 |
| 料金内訳 | 参加者種別別単価×人数・合計 |
| 操作ボタン | 承認 / 拒否（PENDING 時のみ表示）|

#### 4.7.4 予約承認・拒否

**操作**:

| 操作 | ステータス遷移 | 処理 |
|---|---|---|
| 承認（OK） | PENDING → CONFIRMED | 予約者へ確定メール通知 |
| 拒否（NG） | PENDING → CANCELLED | 予約者へキャンセルメール通知・拒否理由入力（任意）|

**拒否時の入力**:
- 拒否理由（任意テキスト）
- 定型文からの選択肢（例: 満員のため / 開催中止のため）

**注意**:
- 承認・拒否後は取り消し不可（変更が必要な場合は Deep Experience 運営者に連絡）
- 承認後に運営者がガイドアサインを行う（既存ワークフロー踏襲）

#### 4.7.5 予約通知

**通知タイミング**:

| イベント | 通知 |
|---|---|
| 新規予約（PENDING）が入った | 画面内通知＋メール通知 |
| 予約がキャンセルされた | 画面内通知 |

**画面内通知**:
- ナビゲーションのベルアイコンにバッジ（未読件数）
- 通知一覧画面で詳細確認・既読処理
- 既存の `Notification` モデルを流用し、事業者向けの通知レコードを生成

**メール通知**:
- 新規予約発生時に事業者の登録メールアドレスへ通知メール送信
- メール通知のON/OFFは通知設定画面で切替可能（4.9 参照）

---

### 4.8 支払い確認

**画面URL**: `/operator/payments`

#### 4.8.1 概要

Deep Experience 運営者から事業者への支払い状況を月単位で確認できる画面。**確定済みの予約に基づく金額のみ**を表示する。

#### 4.8.2 支払い金額の計算ルール

| 項目 | 説明 |
|---|---|
| 事業者の取り分 | 事業者が設定した **1人あたりの利用料金 × 参加人数** の合計 |
| ツアー料金との関係 | Deep Experience 運営者は事業者の利用料金を考慮したうえで、ゲスト向けのツアー料金を別途設定する。差額は運営者の取り分となる。 |
| 手数料控除 | 事業者側の支払い金額からの手数料控除は **なし** |

**計算例**:
```
事業者設定: 大人 3,000円 / 子ども 1,500円
参加者: 大人2名 + 子ども1名
事業者受取額: 3,000×2 + 1,500×1 = 7,500円
```

#### 4.8.3 支払いサイクル設定

支払いサイクルは事業者ごとに個別設定する（Deep Experience 運営者が Admin 管理画面で設定）。

| 設定項目 | 説明 |
|---|---|
| 締め日 | 毎月○日（例: 末日）に当月分を確定 |
| 支払い予定日 | 締め後 翌月○日（例: 翌月25日）に振り込み |

#### 4.8.4 表示仕様

**月別サマリー**:

| 項目 | 説明 |
|---|---|
| 対象年月 | 月選択（セレクトボックスまたは前後ナビゲーション）|
| 対象予約件数 | 当月の確定済み予約件数 |
| 事業者受取額（合計）| 確定済み予約の（利用料金×人数）合計 |
| 支払いステータス | 未払い / 支払済み |
| 支払い予定日 | 事業者ごとの支払いサイクル設定に基づく翌月の支払い予定日 |

**当月対象予約一覧**:

| 列 | 説明 |
|---|---|
| 受注番号 | |
| 開催日 | |
| アクティビティ名 | |
| 参加者内訳 | 種別ごとの人数（例: 大人2名・子ども1名）|
| 利用料金内訳 | 種別ごとの単価 × 人数 |
| 小計 | |
| 確定日 | |

**注意**:
- 「確定済み」とはステータスが `COMPLETED`（ツアー完了）となり、バッチ処理でファイナライズされた予約を指す（既存 `finalizeBookings.ts` バッチ）
- キャンセル・PENDING の予約は含まれない
- 支払い済みになった月のデータは変更不可（読み取り専用）

---

### 4.9 通知設定

**画面URL**: `/operator/settings/notifications`

#### 4.9.1 通知チャンネル

メール送信には **SendGrid** を使用する。将来的に SMS・Slack・LINE 等の通知チャンネルが追加される可能性があるため、通知送信処理は通知チャンネルを抽象化したインターフェース経由で実装し、チャンネル追加時の改修コストを最小化する。

```
NotificationService
├── EmailChannel（SendGrid）← 初期実装
├── （将来）SmsChannel
└── （将来）SlackChannel
```

#### 4.9.2 通知設定項目

| 設定項目 | 種別 | デフォルト |
|---|---|---|
| 新規予約通知（メール）| ON/OFF トグル | ON |
| 新規予約通知（画面内）| ON/OFF トグル | ON |
| キャンセル通知（メール）| ON/OFF トグル | ON |
| キャンセル通知（画面内）| ON/OFF トグル | ON |
| 支払い完了通知（メール）| ON/OFF トグル | ON |
| メール通知の言語 | セレクト（言語マスターに基づく）| 日本語 |

#### 4.9.3 メールテンプレート

SendGrid の Dynamic Templates を使用し、通知種別ごとにテンプレートを管理する。

| 通知種別 | テンプレート内容 |
|---|---|
| 初回パスワード設定 | アカウント発行通知・パスワード設定リンク |
| パスワードリセット | リセットリンク（有効期限1時間）|
| 新規予約通知 | 受注番号・アクティビティ名・開催日時・参加人数・予約者名 |
| 予約キャンセル通知 | 受注番号・キャンセル理由 |
| 支払い完了通知 | 対象年月・件数・金額・振込予定日 |

---

### 4.10 アカウント設定

**画面URL**: `/operator/settings/account`

| 設定項目 | 内容 |
|---|---|
| 担当者名 | テキスト入力・変更可 |
| メールアドレス | 変更時は確認メールを現在・新アドレスの両方へ送信 |
| パスワード変更 | 現パスワード確認の上、新パスワードに変更 |
| 言語設定 | 管理画面の表示言語（日本語/英語）|

---

## 5. 非機能要件

### 5.1 パフォーマンス

| 指標 | 目標値 |
|---|---|
| 画面初期表示（TTI）| 3秒以内 |
| API レスポンスタイム | 95パーセンタイルで1秒以内 |
| 画像アップロード | 10MB以下のファイルを30秒以内で処理・WebP変換 |

### 5.2 ブラウザ対応

- Google Chrome 最新版
- Mozilla Firefox 最新版
- Safari 最新版
- Microsoft Edge 最新版
- モバイルブラウザ（スマートフォン）での基本操作対応

### 5.3 可用性

- 稼働率 99.5% 以上（Vercel / Supabase の SLA に準拠）

---

## 6. セキュリティ要件

| 要件 | 対応方針 |
|---|---|
| 認証 | JWT（httpOnly Cookie）を使用。LocalStorage には保存しない。 |
| 認可 | 全 API エンドポイントで事業者IDによるスコープ制限を実装。IDOR 対策。 |
| パスワード保存 | bcrypt（コストファクター12以上）でハッシュ化 |
| ブルートフォース対策 | 3回連続ログイン失敗で5分間ロック |
| HTTPS | 本番環境は全通信 HTTPS 強制 |
| ファイルアップロード | MIME type チェック（Content-Type とファイルヘッダー両方を検証）。jpg/png/webp のみ許可。 |
| XSS 対策 | React の JSX エスケープを活用。`dangerouslySetInnerHTML` は使用禁止。 |
| CSRF 対策 | `httpOnly` Cookie + `SameSite=Strict` |
| シークレット管理 | API キー・JWT シークレットは環境変数（`.env`）で管理。コードへのハードコーディング禁止。 |

---

## 7. データモデル（追加・変更）

### 7.1 新規追加: `Operator` モデル

事業者アカウントを管理するモデル。

```prisma
model Operator {
  id                     String    @id @default(cuid())
  name                   String                    // 担当者名
  email                  String    @unique
  passwordHash           String
  passwordResetToken     String?
  passwordResetExpiresAt DateTime?
  loginFailureCount      Int       @default(0)
  lockedUntil            DateTime?
  locale                 String    @default("ja")  // 管理画面表示言語

  // 支払いサイクル設定（Deep Experience 運営者が設定）
  paymentClosingDay      Int       @default(31)    // 締め日（1-31、31=末日）
  paymentTransferDay     Int       @default(25)    // 翌月の振込日（1-31）

  // 通知設定
  notifyOnNewBookingEmail  Boolean @default(true)
  notifyOnNewBookingInApp  Boolean @default(true)
  notifyOnCancelEmail      Boolean @default(true)
  notifyOnCancelInApp      Boolean @default(true)
  notifyOnPaymentEmail     Boolean @default(true)
  notifyEmailLocale        String  @default("ja")  // メール通知の言語

  spots                  OperatorSpot[]             // 管理施設（多対多）
  createdAt              DateTime  @default(now())
  updatedAt              DateTime  @updatedAt
}
```

### 7.2 新規追加: `OperatorSpot` モデル（中間テーブル）

事業者と施設（Spot）を多対多で紐付ける。

```prisma
model OperatorSpot {
  operatorId  String
  spotId      String
  operator    Operator @relation(fields: [operatorId], references: [id])
  spot        Spot     @relation(fields: [spotId], references: [id])

  @@id([operatorId, spotId])
}
```

### 7.3 既存モデルへの変更

**`Tour` モデル**:
- `publicationStatus` フィールド追加（`DRAFT` / `PENDING_REVIEW` / `PUBLISHED` / `UNPUBLISHED`）
- 事業者が審査申請できるよう `submittedAt` フィールド追加

**`Spot` モデル**:
- `OperatorSpot[]` リレーション追加

**`Notification` モデル**:
- `operatorId` フィールド追加（事業者向け通知をサポート）

### 7.4 新規追加: `OperatorPaymentRecord` モデル

月次の支払いレコードを管理するモデル。

```prisma
model OperatorPaymentRecord {
  id               String   @id @default(cuid())
  operatorId       String
  operator         Operator @relation(fields: [operatorId], references: [id])
  targetYear       Int                          // 対象年
  targetMonth      Int                          // 対象月（1-12）
  totalAmountCents Int                          // 事業者受取合計（センタ単位）
  bookingCount     Int                          // 対象確定予約件数
  status           PaymentRecordStatus @default(UNPAID)
  scheduledDate    DateTime?                    // 振込予定日
  paidAt           DateTime?                    // 支払い完了日
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  @@unique([operatorId, targetYear, targetMonth])
}

enum PaymentRecordStatus {
  UNPAID    // 未払い
  PAID      // 支払済み
}
```

### 7.5 言語マスター対応

- 既存の i18n リソース（`packages/i18n/src/resources.ts`）に加え、運営者が有効化した言語を DBで管理するための `SupportedLanguage` モデルを追加

```prisma
model SupportedLanguage {
  code      String  @id   // "ja", "en", "zh-CN" 等
  nameJa    String
  nameEn    String
  isActive  Boolean @default(true)
  sortOrder Int     @default(0)
}
```

---

## 8. 画面一覧・遷移

### 8.1 画面一覧

| # | 画面名 | URL |
|---|---|---|
| 1 | ログイン | `/operator/login` |
| 2 | パスワードリセット（メール入力） | `/operator/login/reset-password` |
| 3 | パスワードリセット（新パスワード入力） | `/operator/login/reset-password?token=xxx` |
| 4 | ダッシュボード | `/operator/dashboard` |
| 5 | 施設情報 | `/operator/facility` |
| 6 | アクティビティ一覧 | `/operator/activities` |
| 7 | アクティビティ新規作成 | `/operator/activities/new` |
| 8 | アクティビティ編集 | `/operator/activities/[activityId]` |
| 9 | スケジュール管理 | `/operator/schedules` |
| 10 | 予約カレンダー | `/operator/bookings/calendar` |
| 11 | 予約一覧 | `/operator/bookings` |
| 12 | 予約詳細 | `/operator/bookings/[bookingId]` |
| 13 | 支払い確認 | `/operator/payments` |
| 14 | 通知一覧 | `/operator/notifications` |
| 15 | アカウント設定 | `/operator/settings/account` |
| 16 | 通知設定 | `/operator/settings/notifications` |

### 8.2 主な画面遷移

```
[未ログイン]
  ↓
ログイン画面 ──────────────────────────── パスワードリセット
  │ 認証成功
  ↓
ダッシュボード
  ├── 施設情報
  ├── アクティビティ一覧 → 新規作成 / 編集（基本情報・料金・画像・スケジュール）
  ├── スケジュール管理 → ルール作成 / 休業日設定
  ├── 予約管理
  │     ├── カレンダー ─→ 予約詳細（承認・拒否）
  │     └── 一覧 ──────→ 予約詳細（承認・拒否）
  ├── 支払い確認
  ├── 通知一覧
  └── 設定
        ├── アカウント設定
        └── 通知設定
```

---

*本仕様書は2026-05-18時点の要件に基づいて作成。実装時に詳細が変更される場合は本ドキュメントを随時更新すること。*
