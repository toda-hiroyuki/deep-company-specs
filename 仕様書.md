# deep experience 事業者向け管理画面 仕様書

**バージョン**: 1.0.0  
**作成日**: 2026-05-18  
**対象システム**: deep experience 事業者向け管理画面

---

## 目次

1. [概要](#1-概要)
2. [用語定義](#2-用語定義)
3. [システム構成](#3-システム構成)
4. [認証・アカウント管理](#4-認証アカウント管理)
5. [ダッシュボード](#5-ダッシュボード)
6. [アクティビティ管理（商品設定）](#6-アクティビティ管理商品設定)
7. [予約管理](#7-予約管理)
8. [通知機能](#8-通知機能)
9. [精算・支払い確認](#9-精算支払い確認)
10. [アカウント設定](#10-アカウント設定)
11. [セキュリティ要件](#11-セキュリティ要件)
12. [画面一覧](#12-画面一覧)
13. [API設計](#13-api設計)
14. [データモデル](#14-データモデル)

---

## 1. 概要

### 1.1 目的

本仕様書は、旅行予約サービス **deep experience** におけるガイドツアー向けアクティビティ施設の事業者が利用する管理画面の仕様を定義するものです。

事業者は本管理画面を通じて以下を行うことができます。

- 自社のアクティビティ（商品）の登録・編集
- 予約の確認・承認・拒否
- 支払い状況の確認

### 1.2 対象ユーザー

| ユーザー種別 | 説明 |
|---|---|
| **事業者（施設担当者）** | ガイドツアーに組み込まれるアクティビティを提供する施設の担当者 |
| **deep experience 運営者** | サービス全体を管理する運営チーム（別途管理画面あり） |

### 1.3 スコープ外

- エンドユーザー（観光客）向けの予約フロー
- ガイド向けの管理機能
- アクティビティの**公開操作**（公開は deep experience 運営者が行う）
- 料金の**公開操作**（公開は deep experience 運営者が行う）

---

## 2. 用語定義

| 用語 | 定義 |
|---|---|
| **事業者** | アクティビティを提供する施設・企業 |
| **アクティビティ** | ガイドツアーで訪問・体験できる施設のコンテンツ（例：陶芸体験、茶道体験など） |
| **予約** | エンドユーザーがガイドツアーを通じて行ったアクティビティへの参加申込 |
| **承認（OK）** | 事業者が予約を受け入れること |
| **拒否（NG）** | 事業者が予約を断ること |
| **精算** | deep experience 運営者から事業者への月次支払い |
| **確定分** | キャンセルなく実施が完了した予約に基づく売上 |

---

## 3. システム構成

### 3.1 技術スタック

| レイヤー | 技術 | 備考 |
|---|---|---|
| フロントエンド | Next.js 15（App Router） | SSR + React Client Components |
| バックエンド | Next.js API Routes | RESTful API |
| データベース | PostgreSQL（本番） / SQLite（開発） | Prisma ORM |
| 認証 | JWT（7日間有効） | email + password |
| メール送信 | メール通知サービス | 予約通知・パスワードリセット |

### 3.2 URL設計

```
/company/login                      # ログイン
/company/password-reset             # パスワードリセット申請
/company/password-reset/confirm     # パスワード再設定
/company/dashboard                  # ダッシュボード（ログイン後TOP）
/company/activities                 # アクティビティ一覧
/company/activities/new             # アクティビティ新規作成
/company/activities/[id]            # アクティビティ詳細・編集
/company/bookings                   # 予約一覧（カレンダー）
/company/bookings/[id]              # 予約詳細
/company/payments                   # 精算・支払い確認
/company/settings                   # アカウント設定
```

---

## 4. 認証・アカウント管理

### 4.1 ログイン機能

#### 機能概要
事業者がメールアドレスとパスワードを使って管理画面にログインできる。

#### 画面仕様（C-01: ログイン画面）

| 項目 | 仕様 |
|---|---|
| メールアドレス入力欄 | テキスト入力、必須、メール形式バリデーション |
| パスワード入力欄 | パスワード入力（マスク表示）、必須 |
| 表示/非表示切り替え | パスワード入力欄の右側にアイコンボタン |
| ログインボタン | クリックで認証処理を実行 |
| パスワードを忘れた方へ | パスワードリセット申請画面へのリンク |

#### 認証仕様

- 認証成功時：JWTトークンを発行し、`httpOnly` Cookie または `localStorage` に保存
- 認証失敗時：「メールアドレスまたはパスワードが正しくありません」を表示（どちらが誤りかは明示しない）
- 連続失敗（5回）：15分間のアカウントロック（ブルートフォース対策）
- ログイン済みの場合：`/company/dashboard` へリダイレクト
- JWTの有効期限：7日間
- セッション切れ時：自動的にログイン画面へリダイレクト

### 4.2 パスワードリセット機能

#### 画面仕様（C-02: パスワードリセット申請画面）

| 項目 | 仕様 |
|---|---|
| メールアドレス入力欄 | テキスト入力、必須、メール形式バリデーション |
| 送信ボタン | クリックでリセットメールを送信 |
| ログイン画面へ戻る | ログイン画面へのリンク |

#### 処理仕様

1. 登録済みメールアドレスを入力して送信
2. 該当アドレスが存在する場合、パスワードリセット用URLをメール送信
3. 該当アドレスが存在しない場合も同じメッセージを表示（アカウント存在確認攻撃対策）
4. メール内URLの有効期限：1時間
5. URLは1回使用のみ有効（使用後無効化）

#### 画面仕様（C-03: パスワード再設定画面）

| 項目 | 仕様 |
|---|---|
| 新しいパスワード | テキスト入力（マスク）、必須 |
| 新しいパスワード（確認） | テキスト入力（マスク）、必須、一致チェック |
| 設定ボタン | クリックでパスワード更新 |

**パスワードポリシー**:
- 8文字以上
- 英字（大文字・小文字）、数字をそれぞれ1文字以上含む
- 記号（`!@#$%^&*`など）を1文字以上含むことを推奨

### 4.3 ログアウト機能

- ヘッダーのメニューからいつでもログアウト可能
- ログアウト時：JWTトークンを無効化し、ログイン画面へリダイレクト
- セッション切れ時も同様にログイン画面へ誘導

---

## 5. ダッシュボード

### 5.1 画面仕様（C-04: ダッシュボード）

ログイン後のトップ画面。自社の状況を一目で把握できる。

#### サマリーカード

| カード名 | 表示内容 |
|---|---|
| 今月の予約数 | 当月の確認済み予約件数 |
| 未対応の予約 | 承認・拒否を行っていない予約件数（要対応） |
| 今月の売上見込み | 確認済み予約の合計金額（参考値） |
| 登録アクティビティ数 | 登録済みアクティビティの総数（公開中/審査中の内訳付き） |

#### 直近の予約一覧

- 直近7日間の予約を最大5件表示
- 予約日時・アクティビティ名・参加人数・ステータスを表示
- 「予約一覧を見る」ボタンで予約管理画面へ遷移

#### 承認待ちの予約

- 承認・拒否が未実施の予約を最大5件表示
- 期限が近いものを優先表示
- 各予約に「承認」「拒否」ボタンを表示（クイックアクション）

---

## 6. アクティビティ管理（商品設定）

### 6.1 アクティビティ一覧（C-05）

#### 表示項目

| 項目 | 内容 |
|---|---|
| サムネイル画像 | 代表画像（100×80px） |
| アクティビティ名 | 日本語名称 |
| 料金 | 1人あたりの利用料金 |
| ステータス | 下書き / 審査中 / 公開中 / 非公開 |
| 更新日時 | 最終更新日 |
| 操作 | 編集ボタン |

#### フィルタ・検索

- ステータスによるフィルタリング
- キーワード検索（アクティビティ名）

#### ステータス遷移

```
[下書き] ──→ [審査待ち] ──→（運営者が審査）──→ [公開中]
                ↑                                    ↓
                └──────────（差し戻し）──────────────┘
                                                     ↓
                                              [非公開]（運営者操作）
```

**事業者が操作できるステータス**:
- `下書き` → `審査待ち`（申請ボタン）
- `審査待ち` → `下書き`（申請取り消し）

**運営者のみが操作できるステータス**:
- `審査待ち` → `公開中`
- `公開中` → `非公開`
- 差し戻し

### 6.2 アクティビティ新規作成・編集（C-06）

#### 基本情報

| 項目 | 必須 | 仕様 |
|---|---|---|
| アクティビティ名（日本語） | ○ | テキスト、最大100文字 |
| アクティビティ名（英語） | ○ | テキスト、最大100文字 |
| 説明文（日本語） | ○ | テキストエリア、最大2000文字 |
| 説明文（英語） | ○ | テキストエリア、最大2000文字 |
| カテゴリ | ○ | セレクト（文化体験 / 食体験 / アウトドア / ものづくり / その他） |
| 所要時間 | ○ | 数値入力（分単位）、10分〜480分 |
| 最少催行人数 | ○ | 数値入力（1〜50人） |
| 最大受け入れ人数 | ○ | 数値入力（1〜100人） |
| 住所 | ○ | テキスト（都道府県・市区町村・番地） |
| アクセス情報（日本語） | - | テキストエリア、最大500文字 |
| アクセス情報（英語） | - | テキストエリア、最大500文字 |

#### 料金設定

| 項目 | 必須 | 仕様 |
|---|---|---|
| 大人料金（1名あたり） | ○ | 数値入力（円）、0〜100,000円 |
| 子供料金（1名あたり） | - | 数値入力（円）、設定しない場合は「大人と同額」 |
| 幼児料金（1名あたり） | - | 数値入力（円）、設定しない場合は「無料」 |

> **注意**: 料金の変更・設定後、公開への反映は deep experience 運営者が行います。設定後に「公開申請」ボタンより申請してください。

#### 受付時間・定休日

| 項目 | 必須 | 仕様 |
|---|---|---|
| 営業時間（開始） | ○ | 時間選択（15分単位） |
| 営業時間（終了） | ○ | 時間選択（15分単位） |
| 定休日 | - | 曜日の複数選択 |
| 特別休業日 | - | カレンダーから日付を複数選択 |
| 予約受付締切 | ○ | 数値入力（〇日前・〇時間前） |

#### 画像管理

- メイン画像：1枚（必須）、JPEG/PNG、最大5MB
- サブ画像：最大9枚、JPEG/PNG、各最大5MB
- ドラッグ&ドロップで順番を変更可能
- アップロード後にトリミング可能

#### ハイライト・注意事項

| 項目 | 必須 | 仕様 |
|---|---|---|
| ハイライト（日本語） | - | 箇条書き形式、最大10件 |
| ハイライト（英語） | - | 箇条書き形式、最大10件 |
| 含まれるもの（日本語） | - | 箇条書き形式、最大10件 |
| 含まれるもの（英語） | - | 箇条書き形式、最大10件 |
| 注意事項（日本語） | - | テキストエリア、最大1000文字 |
| 注意事項（英語） | - | テキストエリア、最大1000文字 |

#### 保存・申請

| ボタン | 動作 |
|---|---|
| 下書き保存 | バリデーションなしで保存（ステータス：下書き） |
| 公開申請 | バリデーション実施後、ステータスを「審査待ち」に変更 |

---

## 7. 予約管理

### 7.1 予約カレンダー（C-07）

#### 表示仕様

- **月表示**（デフォルト）・週表示・日表示 を切り替え可能
- 各日付のセルに当日の予約件数を表示
- 予約がある日はカラーハイライト（ステータス別に色分け）

**ステータス色分け**:

| ステータス | 色 | 説明 |
|---|---|---|
| 承認待ち | オレンジ | 事業者の対応が必要 |
| 承認済み | グリーン | 実施予定 |
| 拒否済み | レッド | 受け入れ不可 |
| 実施済み | グレー | 完了 |
| キャンセル | ライトグレー | キャンセル済み |

#### 予約一覧（リスト表示）

カレンダー下部または切り替えボタンでリスト形式での表示も可能。

| 表示項目 | 内容 |
|---|---|
| 予約日時 | アクティビティの実施日時 |
| 予約番号 | システム発行の一意の番号 |
| アクティビティ名 | 対象アクティビティ |
| 参加人数 | 大人○名・子供○名 |
| 代表者名 | 予約者の名前（匿名化対応） |
| ステータス | 承認待ち / 承認済み / 拒否済み / 実施済み / キャンセル |
| 操作 | 詳細ボタン |

#### フィルタ

- 対象アクティビティで絞り込み
- ステータスで絞り込み
- 日付範囲で絞り込み

### 7.2 予約詳細（C-08）

#### 表示項目

| 項目 | 内容 |
|---|---|
| 予約番号 | 一意の識別番号 |
| 予約日時 | 申込が行われた日時 |
| アクティビティ実施日時 | 参加予定の日時 |
| アクティビティ名 | 対象アクティビティ |
| 参加人数 | 大人・子供・幼児の人数 |
| 合計金額 | 参加費用の合計（参考値） |
| 特別なリクエスト | 予約者からのコメント |
| ステータス | 現在のステータス |
| 対応期限 | 承認/拒否の期限（実施日の前日まで等） |

#### 操作ボタン

| ボタン | 表示条件 | 動作 |
|---|---|---|
| **承認（OK）** | ステータスが「承認待ち」 | 予約を承認し、ステータスを「承認済み」に変更 |
| **拒否（NG）** | ステータスが「承認待ち」 | 拒否理由を入力後、ステータスを「拒否済み」に変更 |

#### 承認・拒否の仕様

**承認時**:
- 確認ダイアログ表示
- 承認後、予約者（ガイド経由）に通知メール送信
- ステータス：`承認待ち` → `承認済み`

**拒否時**:
- 拒否理由の入力モーダル表示（必須入力）
- 拒否理由の選択肢（例：満員、設備メンテナンス、定休日）+ 自由記述
- 拒否後、予約者（ガイド経由）に通知メール送信
- ステータス：`承認待ち` → `拒否済み`

**対応期限**:
- アクティビティ実施日の**2日前**を対応期限とする（システム設定で変更可能）
- 期限超過後は自動的に「承認済み」に変更（デフォルト動作。設定で「自動拒否」も選択可）

---

## 8. 通知機能

### 8.1 通知の種類

| 通知種別 | タイミング | 通知方法 |
|---|---|---|
| 予約新規入電 | 自社アクティビティに新しい予約が入ったとき | メール + 画面内通知 |
| 承認期限リマインダー | 未対応の予約の対応期限1日前 | メール + 画面内通知 |
| 予約キャンセル | 承認済み予約がキャンセルされたとき | メール + 画面内通知 |
| 運営者からのメッセージ | deep experience 運営者からのお知らせ | メール + 画面内通知 |
| アクティビティ審査結果 | 申請したアクティビティの審査が完了したとき | メール + 画面内通知 |

### 8.2 通知設定（C-10 アカウント設定内）

各通知種別について以下の設定が可能:

- メール通知の ON / OFF
- 画面内通知の ON / OFF
- 通知先メールアドレス（複数登録可能、最大3件）

### 8.3 画面内通知（C-09: 通知一覧）

- ヘッダーに通知アイコンと未読件数バッジを表示
- クリックで通知ドロワーを展開し直近10件を表示
- 「すべての通知を見る」で通知一覧画面へ遷移
- 通知一覧では日付・種別フィルタ、既読/未読フィルタが利用可能
- 通知クリックで関連する予約・アクティビティへ遷移

---

## 9. 精算・支払い確認

### 9.1 精算一覧（C-10）

#### 表示仕様

月ごとの精算情報を一覧表示する。

| 表示項目 | 内容 |
|---|---|
| 対象月 | 精算対象の月（YYYY年MM月） |
| 確定予約数 | その月に実施完了した予約数 |
| 精算金額 | deep experience からの支払い予定額 |
| ステータス | 精算中 / 支払い済み |
| 明細 | 明細確認ボタン |

**表示対象**:
- **確定分のみ**表示（実施完了済みの予約のみ）
- キャンセル・拒否済みは含まない
- 現在月は「精算中（未確定）」として表示し、確定額が確定次第更新

### 9.2 精算明細（C-11）

選択した月の精算明細を表示する。

| 表示項目 | 内容 |
|---|---|
| 予約番号 | 各予約の識別番号 |
| アクティビティ名 | 対象アクティビティ |
| 実施日時 | ツアー実施日時 |
| 参加人数 | 人数内訳 |
| 単価 | 参加者区分ごとの料金 |
| 小計 | 予約ごとの金額 |
| 合計 | 月次合計金額 |

- PDF形式でダウンロード可能
- CSV形式でエクスポート可能（会計処理用）

---

## 10. アカウント設定

### 10.1 プロフィール設定（C-12）

#### 事業者情報

| 項目 | 必須 | 仕様 |
|---|---|---|
| 事業者名（日本語） | ○ | テキスト、最大100文字 |
| 事業者名（英語） | ○ | テキスト、最大100文字 |
| 代表者名 | ○ | テキスト、最大50文字 |
| 電話番号 | ○ | 電話番号形式バリデーション |
| 登録メールアドレス | ○ | 変更時は確認メール送信 |
| 住所 | ○ | 都道府県・市区町村・番地 |
| 事業者ロゴ | - | JPEG/PNG、最大2MB |
| 事業者紹介文（日本語） | - | テキストエリア、最大500文字 |
| 事業者紹介文（英語） | - | テキストエリア、最大500文字 |

#### 振込先情報

| 項目 | 必須 | 仕様 |
|---|---|---|
| 銀行名 | ○ | テキスト |
| 支店名 | ○ | テキスト |
| 口座種別 | ○ | 普通 / 当座 |
| 口座番号 | ○ | 数値、7桁 |
| 口座名義（カタカナ） | ○ | テキスト、カタカナバリデーション |

> 振込先情報は deep experience 運営者のみ参照可能。画面上では一部マスク表示。

### 10.2 パスワード変更（C-12 内）

| 項目 | 必須 | 仕様 |
|---|---|---|
| 現在のパスワード | ○ | テキスト（マスク） |
| 新しいパスワード | ○ | テキスト（マスク）、パスワードポリシー準拠 |
| 新しいパスワード（確認） | ○ | テキスト（マスク）、一致チェック |

### 10.3 通知設定（C-12 内）

「8.2 通知設定」を参照。

---

## 11. セキュリティ要件

### 11.1 アクセス制御

- ログインしていないユーザーは `/company/*` 配下のすべてのページにアクセス不可（ログイン画面へリダイレクト）
- 事業者Aは事業者Bのアクティビティ・予約・精算情報にアクセス不可
- すべてのAPIエンドポイントでJWT認証を必須とし、サーバーサイドで事業者IDを検証

### 11.2 データ保護

- パスワードは bcrypt（コストファクター12以上）でハッシュ化して保存
- 振込先情報はAES-256等で暗号化して保存
- 個人情報（予約者名・連絡先）は参照権限を最小化
- 通信はHTTPS必須

### 11.3 セッション管理

- JWTの有効期限：7日間
- ログアウト時にサーバーサイドでトークン無効化（ブラックリスト管理またはリフレッシュトークン方式）
- 同一アカウントで複数デバイスからのログインは許可（セキュリティ設定で制限可能）
- 長時間操作なし（30分）でセッション警告を表示

### 11.4 入力値検証

- すべての入力値はサーバーサイドでバリデーション（Zodスキーマ使用）
- SQLインジェクション対策：Prisma ORMのパラメータバインディングを使用
- XSS対策：出力エスケープ、CSP設定
- CSRF対策：SameSite Cookie + CSRFトークン

---

## 12. 画面一覧

| 画面ID | 画面名 | URL | 認証 |
|---|---|---|---|
| C-01 | ログイン | `/company/login` | 不要 |
| C-02 | パスワードリセット申請 | `/company/password-reset` | 不要 |
| C-03 | パスワード再設定 | `/company/password-reset/confirm` | 不要 |
| C-04 | ダッシュボード | `/company/dashboard` | 必要 |
| C-05 | アクティビティ一覧 | `/company/activities` | 必要 |
| C-06 | アクティビティ作成・編集 | `/company/activities/[id]` | 必要 |
| C-07 | 予約カレンダー・一覧 | `/company/bookings` | 必要 |
| C-08 | 予約詳細 | `/company/bookings/[id]` | 必要 |
| C-09 | 通知一覧 | `/company/notifications` | 必要 |
| C-10 | 精算一覧 | `/company/payments` | 必要 |
| C-11 | 精算明細 | `/company/payments/[yearMonth]` | 必要 |
| C-12 | アカウント設定 | `/company/settings` | 必要 |

---

## 13. API設計

### 13.1 認証

| メソッド | エンドポイント | 概要 |
|---|---|---|
| `POST` | `/api/v1/company/auth/login` | ログイン |
| `POST` | `/api/v1/company/auth/logout` | ログアウト |
| `POST` | `/api/v1/company/auth/password-reset/request` | パスワードリセット申請 |
| `POST` | `/api/v1/company/auth/password-reset/confirm` | パスワード再設定 |
| `POST` | `/api/v1/company/auth/change-password` | パスワード変更（ログイン中） |

### 13.2 アクティビティ（商品）

| メソッド | エンドポイント | 概要 |
|---|---|---|
| `GET` | `/api/v1/company/activities` | アクティビティ一覧取得 |
| `POST` | `/api/v1/company/activities` | アクティビティ新規作成 |
| `GET` | `/api/v1/company/activities/:id` | アクティビティ詳細取得 |
| `PUT` | `/api/v1/company/activities/:id` | アクティビティ更新 |
| `DELETE` | `/api/v1/company/activities/:id` | アクティビティ削除（下書きのみ） |
| `POST` | `/api/v1/company/activities/:id/submit` | 公開申請 |
| `POST` | `/api/v1/company/activities/:id/cancel-submit` | 申請取り消し |
| `POST` | `/api/v1/company/activities/:id/media` | 画像アップロード |
| `DELETE` | `/api/v1/company/activities/:id/media/:mediaId` | 画像削除 |

### 13.3 予約

| メソッド | エンドポイント | 概要 |
|---|---|---|
| `GET` | `/api/v1/company/bookings` | 予約一覧取得（日付・ステータスフィルタ対応） |
| `GET` | `/api/v1/company/bookings/:id` | 予約詳細取得 |
| `POST` | `/api/v1/company/bookings/:id/approve` | 予約承認 |
| `POST` | `/api/v1/company/bookings/:id/reject` | 予約拒否（拒否理由必須） |

### 13.4 通知

| メソッド | エンドポイント | 概要 |
|---|---|---|
| `GET` | `/api/v1/company/notifications` | 通知一覧取得 |
| `POST` | `/api/v1/company/notifications/:id/read` | 通知既読化 |
| `POST` | `/api/v1/company/notifications/read-all` | 全通知既読化 |

### 13.5 精算

| メソッド | エンドポイント | 概要 |
|---|---|---|
| `GET` | `/api/v1/company/payments` | 精算一覧取得 |
| `GET` | `/api/v1/company/payments/:yearMonth` | 月次精算明細取得 |
| `GET` | `/api/v1/company/payments/:yearMonth/export` | 明細PDF/CSVエクスポート |

### 13.6 アカウント設定

| メソッド | エンドポイント | 概要 |
|---|---|---|
| `GET` | `/api/v1/company/profile` | プロフィール取得 |
| `PUT` | `/api/v1/company/profile` | プロフィール更新 |
| `PUT` | `/api/v1/company/profile/bank` | 振込先情報更新 |
| `GET` | `/api/v1/company/notification-settings` | 通知設定取得 |
| `PUT` | `/api/v1/company/notification-settings` | 通知設定更新 |

---

## 14. データモデル

### 14.1 CompanyAccount（事業者アカウント）

```prisma
model CompanyAccount {
  id               String    @id @default(cuid())
  email            String    @unique
  passwordHash     String
  companyNameJa    String
  companyNameEn    String
  representativeName String
  phone            String
  addressPrefecture String
  addressCity      String
  addressStreet    String
  logoMediaId      String?
  descriptionJa    String?
  descriptionEn    String?

  // 振込先情報（暗号化保存）
  bankName         String?
  branchName       String?
  accountType      String?   // "ORDINARY" | "CHECKING"
  accountNumber    String?   // 暗号化
  accountHolder    String?   // 暗号化

  // パスワードリセット
  passwordResetToken     String?
  passwordResetExpiresAt DateTime?

  // ログイン失敗
  loginFailureCount Int      @default(0)
  lockedUntil       DateTime?

  createdAt        DateTime  @default(now())
  updatedAt        DateTime  @updatedAt

  activities       CompanyActivity[]
  notifications    CompanyNotification[]
  notificationSettings CompanyNotificationSettings?
}
```

### 14.2 CompanyActivity（事業者アクティビティ）

```prisma
model CompanyActivity {
  id                   String   @id @default(cuid())
  companyId            String
  company              CompanyAccount @relation(fields: [companyId], references: [id])

  nameJa               String
  nameEn               String
  descriptionJa        String
  descriptionEn        String
  category             String   // "CULTURE" | "FOOD" | "OUTDOOR" | "CRAFT" | "OTHER"
  durationMinutes      Int
  minParticipants      Int
  maxParticipants      Int

  // 住所・アクセス
  addressPrefecture    String
  addressCity          String
  addressStreet        String
  accessInfoJa         String?
  accessInfoEn         String?

  // 料金（円）
  priceAdult           Int
  priceChild           Int?
  priceInfant          Int?     @default(0)

  // 営業時間・定休日
  openTime             String   // "HH:MM"
  closeTime            String   // "HH:MM"
  closedDays           String   // JSON: ["MONDAY","TUESDAY",...]
  specialClosedDates   String   // JSON: ["YYYY-MM-DD",...]
  bookingCutoffHours   Int      // 受付締切（時間前）

  // コンテンツ
  highlightsJa         String?  // JSON array
  highlightsEn         String?  // JSON array
  inclusionsJa         String?  // JSON array
  inclusionsEn         String?  // JSON array
  notesJa              String?
  notesEn              String?

  // ステータス
  status               String   @default("DRAFT")
  // "DRAFT" | "PENDING_REVIEW" | "PUBLISHED" | "UNPUBLISHED"

  // 審査
  submittedAt          DateTime?
  reviewedAt           DateTime?
  reviewNote           String?

  createdAt            DateTime @default(now())
  updatedAt            DateTime @updatedAt

  bookings             CompanyBooking[]
  media                CompanyActivityMedia[]
}
```

### 14.3 CompanyBooking（事業者予約）

```prisma
model CompanyBooking {
  id                   String   @id @default(cuid())
  bookingNumber        String   @unique
  activityId           String
  activity             CompanyActivity @relation(fields: [activityId], references: [id])

  scheduledAt          DateTime // アクティビティ実施日時
  participantsAdult    Int      @default(0)
  participantsChild    Int      @default(0)
  participantsInfant   Int      @default(0)
  totalAmountJpy       Int      // 合計金額（円）

  specialRequests      String?

  status               String   @default("PENDING")
  // "PENDING" | "APPROVED" | "REJECTED" | "COMPLETED" | "CANCELLED"

  rejectionReason      String?
  approvedAt           DateTime?
  rejectedAt           DateTime?
  approvalDeadline     DateTime // 対応期限

  // 予約者情報（ガイドツアー経由のため匿名化可能）
  bookerName           String?
  bookerEmail          String?

  createdAt            DateTime @default(now())
  updatedAt            DateTime @updatedAt
}
```

### 14.4 CompanyNotification（通知）

```prisma
model CompanyNotification {
  id          String   @id @default(cuid())
  companyId   String
  company     CompanyAccount @relation(fields: [companyId], references: [id])

  type        String
  // "NEW_BOOKING" | "APPROVAL_REMINDER" | "BOOKING_CANCELLED"
  // | "OPERATOR_MESSAGE" | "REVIEW_RESULT"

  title       String
  message     String
  relatedId   String?  // 関連するbookingIdまたはactivityId
  isRead      Boolean  @default(false)
  readAt      DateTime?

  createdAt   DateTime @default(now())
}
```

### 14.5 CompanyPayment（精算）

```prisma
model CompanyPayment {
  id               String   @id @default(cuid())
  companyId        String
  yearMonth        String   // "YYYY-MM"
  totalAmountJpy   Int      // 精算金額（円）
  bookingCount     Int      // 対象予約数
  status           String   // "PROCESSING" | "PAID"
  paidAt           DateTime?

  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt
}
```

---

## 改訂履歴

| バージョン | 日付 | 変更内容 |
|---|---|---|
| 1.0.0 | 2026-05-18 | 初版作成 |
